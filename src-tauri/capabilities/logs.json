{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "logs-window",
  "description": "Permissions for the separate game-logs window. It loads its content from the Vite dev server URL (WebviewUrl::External) rather than the bundled app origin, so it needs an explicit remote-URL allowance to get Tauri IPC at all.",
  "windows": ["logs"],
  "remote": {
    "urls": ["http://localhost:5173/*"]
  },
  "permissions": [
    "core:default",
    "core:window:allow-minimize",
    "core:window:allow-toggle-maximize",
    "core:window:allow-close",
    "core:window:allow-destroy",
    "core:window:allow-is-maximized",
    "core:window:allow-start-dragging"
  ]
}

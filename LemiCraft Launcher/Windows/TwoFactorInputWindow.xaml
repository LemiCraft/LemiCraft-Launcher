<Window x:Class="LemiCraft_Launcher.Windows.TwoFactorInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Двухфакторная аутентификация"
        Height="Auto"
        Width="420"
        SizeToContent="Height"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen">

    <Border x:Name="RootBorder"
            Background="#0F172A"
            CornerRadius="12"
            BorderBrush="#374151"
            BorderThickness="1"
            RenderTransformOrigin="0.5,0.5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="#1E293B"
                    CornerRadius="12,12,0,0"
                    MouseLeftButtonDown="TitleBar_MouseDown">
                <Grid Margin="20,14">
                    <TextBlock Text="Двухфакторная аутентификация"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="White"
                               VerticalAlignment="Center"/>

                    <Button Content="×"
                            Width="24"
                            Height="24"
                            HorizontalAlignment="Right"
                            FontSize="18"
                            Background="Transparent"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="CancelButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border"
                                        Background="{TemplateBinding Background}"
                                        CornerRadius="4">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#374151"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>

            <StackPanel Grid.Row="1" Margin="24,20,24,4">
                <TextBlock Text="У аккаунта включена двухфакторная аутентификация. Введите код из приложения-аутентификатора:"
                           FontSize="14"
                           Foreground="#E5E7EB"
                           TextWrapping="Wrap"
                           LineHeight="20"
                           Margin="0,0,0,16"/>

                <TextBox x:Name="CodeTextBox"
                         Height="44"
                         Padding="14,0"
                         FontSize="20"
                         FontWeight="SemiBold"
                         MaxLength="10"
                         Background="#1E293B"
                         Foreground="White"
                         CaretBrush="White"
                         BorderBrush="#374151"
                         BorderThickness="1"
                         VerticalContentAlignment="Center"
                         HorizontalContentAlignment="Center"
                         KeyDown="CodeTextBox_KeyDown">
                    <TextBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>

                <TextBlock x:Name="ErrorText"
                           Text=""
                           FontSize="12"
                           Foreground="#EF4444"
                           TextWrapping="Wrap"
                           Margin="2,8,0,0"
                           Visibility="Collapsed"/>
            </StackPanel>

            <Grid Grid.Row="2" Margin="24,16,24,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="CancelButton"
                        Grid.Column="1"
                        Content="Отмена"
                        Height="38"
                        MinWidth="100"
                        Padding="20,0"
                        Margin="0,0,8,0"
                        Background="Transparent"
                        Foreground="White"
                        BorderBrush="#374151"
                        BorderThickness="1"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Cursor="Hand"
                        Click="CancelButton_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#374151"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Button x:Name="ConfirmButton"
                        Grid.Column="2"
                        Content="Подтвердить"
                        Height="38"
                        MinWidth="120"
                        Padding="20,0"
                        Background="#22C55E"
                        Foreground="White"
                        BorderThickness="0"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Cursor="Hand"
                        Click="ConfirmButton_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#16A34A"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>

<Window x:Class="LemiCraft_Launcher.Windows.SkinDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Детали скина"
        Width="550"
        Height="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        ShowInTaskbar="False">

    <Border x:Name="RootBorder"
            Background="#1E293B" 
            CornerRadius="16" 
            BorderBrush="#374151" 
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.3" BlurRadius="20" ShadowDepth="0"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" 
                    Background="#0F172A"
                    CornerRadius="16,16,0,0"
                    MouseLeftButtonDown="TitleBar_MouseDown">
                <Grid Height="50" Margin="20,0">
                    <TextBlock x:Name="TitleText"
                               Text="Детали скина"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="White"
                               VerticalAlignment="Center"/>

                    <Button Content="✕"
                            Width="32"
                            Height="32"
                            Padding="0"
                            FontSize="16"
                            Foreground="White"
                            Background="Transparent"
                            BorderThickness="0"
                            HorizontalAlignment="Right"
                            Cursor="Hand"
                            Click="CloseButton_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border x:Name="border"
                                                    Background="{TemplateBinding Background}"
                                                    CornerRadius="6">
                                                <ContentPresenter HorizontalAlignment="Center" 
                                                                VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="border" Property="Background" Value="#EF4444"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <ScrollViewer Grid.Row="1" 
                          VerticalScrollBarVisibility="Auto"
                          Margin="30,20">
                <StackPanel>
                    <Border Background="#0F172A"
                            CornerRadius="12"
                            Height="350"
                            Margin="0,0,0,20">
                        <Grid>
                            <Border Background="#1E293B">
                                <Border.OpacityMask>
                                    <DrawingBrush TileMode="Tile" Viewport="0,0,20,20" ViewportUnits="Absolute">
                                        <DrawingBrush.Drawing>
                                            <GeometryDrawing Brush="#374151">
                                                <GeometryDrawing.Geometry>
                                                    <RectangleGeometry Rect="0,0,10,10"/>
                                                </GeometryDrawing.Geometry>
                                            </GeometryDrawing>
                                        </DrawingBrush.Drawing>
                                    </DrawingBrush>
                                </Border.OpacityMask>
                            </Border>

                            <Grid>
                                <Image x:Name="SkinPreview"
                                       Stretch="Uniform"
                                       RenderOptions.BitmapScalingMode="NearestNeighbor"
                                       Visibility="Collapsed" />

                                <StackPanel x:Name="LoadingPanel"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Visibility="Visible">
                                    <TextBlock Text="&#xE916;"
                                               FontFamily="Segoe MDL2 Assets"
                                               Foreground="White"
                                               FontSize="72"
                                               HorizontalAlignment="Center"
                                               Margin="0,0,0,20"
                                               RenderTransformOrigin="0.5,0.5">
                                        <TextBlock.RenderTransform>
                                            <RotateTransform x:Name="LoadingRotation"/>
                                        </TextBlock.RenderTransform>
                                        <TextBlock.Triggers>
                                            <EventTrigger RoutedEvent="Loaded">
                                                <BeginStoryboard>
                                                    <Storyboard RepeatBehavior="Forever">
                                                        <DoubleAnimation Storyboard.TargetName="LoadingRotation"
                                                                         Storyboard.TargetProperty="Angle"
                                                                         From="0"
                                                                         To="360"
                                                                         Duration="0:0:2"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </EventTrigger>
                                        </TextBlock.Triggers>
                                    </TextBlock>

                                    <TextBlock x:Name="LoadingText"
                                               Text="Генерация 3D рендера..."
                                               FontSize="16"
                                               Foreground="#9CA3AF"
                                               HorizontalAlignment="Center"
                                               Margin="0,0,0,8"/>

                                    <TextBlock x:Name="LoadingSubText"
                                               Text="Это может занять несколько секунд"
                                               FontSize="12"
                                               Foreground="#6B7280"
                                               HorizontalAlignment="Center"/>
                                </StackPanel>

                                <StackPanel x:Name="ErrorPanel"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Visibility="Collapsed">
                                    <TextBlock Text="&#xE8F1;"
                                               FontFamily="Segoe MDL2 Assets"
                                               Foreground="#6B7280"
                                               FontSize="72"
                                               HorizontalAlignment="Center"
                                               Margin="0,0,0,20"/>

                                    <TextBlock Text="Не удалось загрузить превью"
                                               FontSize="16"
                                               Foreground="#9CA3AF"
                                               HorizontalAlignment="Center"
                                               Margin="0,0,0,8"/>

                                    <Button Click="RetryLoadingButton_Click"
                                            Background="#374151"
                                            Foreground="White"
                                            BorderThickness="0"
                                            Padding="16,8"
                                            FontSize="13"
                                            Cursor="Hand"
                                            HorizontalAlignment="Center">
                                        <Button.Content>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="&#xE72C;" FontFamily="Segoe MDL2 Assets" FontSize="13" Foreground="White" Margin="0,0,7,0" VerticalAlignment="Center"/>
                                                <TextBlock Text="Попробовать снова" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Button.Content>
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}"
                                                        Padding="{TemplateBinding Padding}"
                                                        CornerRadius="8">
                                                    <ContentPresenter HorizontalAlignment="Center"
                                                                      VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </Border>

                    <TextBlock x:Name="SkinName"
                               Text="Skin Name"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="White"
                               Margin="0,0,0,16"/>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                        <Border Background="#22C55E"
                                CornerRadius="8"
                                Padding="12,6"
                                Margin="0,0,10,0">
                            <TextBlock x:Name="ModelBadge"
                                       Text="ТОЛСТЫЕ РУКИ"
                                       FontSize="12"
                                       FontWeight="Bold"
                                       Foreground="White"/>
                        </Border>

                        <Border x:Name="ActiveBadge"
                                Background="#3B82F6"
                                CornerRadius="8"
                                Padding="12,6"
                                Visibility="Collapsed">
                            <TextBlock Text="✓ Активен"
                                       FontSize="12"
                                       FontWeight="Bold"
                                       Foreground="White"/>
                        </Border>
                    </StackPanel>

                    <Border Background="#0F172A"
                            CornerRadius="12"
                            Padding="16">
                        <TextBlock x:Name="DateText"
                                   FontSize="13"
                                   Foreground="#9CA3AF"/>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <Grid Grid.Row="2" 
                  Margin="30,0,30,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button x:Name="DeleteButton"
                        Grid.Column="0"
                        Background="#EF4444"
                        Foreground="White"
                        BorderThickness="0"
                        Height="44"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Cursor="Hand"
                        Click="DeleteButton_Click">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE74D;" FontFamily="Segoe MDL2 Assets" FontSize="14" Foreground="White" Margin="0,0,7,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Удалить" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button.Content>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border"
                                                Background="{TemplateBinding Background}"
                                                Padding="16,10"
                                                CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#DC2626"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <Button x:Name="ApplyButton"
                        Grid.Column="1"
                        Background="#22C55E"
                        Foreground="White"
                        BorderThickness="0"
                        Width="140"
                        Height="44"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Margin="10,0"
                        Cursor="Hand"
                        Click="ApplyButton_Click">
                    <Button.Content>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE73E;" FontFamily="Segoe MDL2 Assets" FontSize="14" Foreground="White" Margin="0,0,7,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Применить" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button.Content>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border"
                                                Background="{TemplateBinding Background}"
                                                Padding="16,10"
                                                CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#16A34A"/>
                                            </Trigger>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter TargetName="border" Property="Background" Value="#6B7280"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Grid.Column="2"
                        Content="Закрыть"
                        Background="#374151"
                        Foreground="White"
                        BorderThickness="0"
                        Width="100"
                        Height="44"
                        FontSize="14"
                        FontWeight="SemiBold"
                        Cursor="Hand"
                        Click="CloseButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border"
                                                Background="{TemplateBinding Background}"
                                                Padding="16,10"
                                                CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center" 
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#4B5563"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>

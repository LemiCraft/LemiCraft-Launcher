<Window x:Class="LemiCraft_Launcher.Windows.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Обновление лаунчера"
        Height="520"
        Width="560"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStartupLocation="CenterOwner">

    <Border x:Name="RootBorder"
            Background="#0F172A"
            CornerRadius="16"
            BorderBrush="#1E293B"
            BorderThickness="1"
            RenderTransformOrigin="0.5,0.5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="#0F172A"
                    CornerRadius="16,16,0,0"
                    BorderBrush="#1E293B"
                    BorderThickness="0,0,0,1"
                    MouseLeftButtonDown="TitleBar_MouseDown">
                <Grid Margin="20,0" Height="52">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Width="8" Height="8" CornerRadius="4" Background="#22C55E" Margin="0,0,10,0"/>
                        <TextBlock Text="Доступно обновление"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="#CBD5E1"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <Button x:Name="CloseButtonTop"
                            Width="28" Height="28"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Cursor="Hand"
                            Click="CloseButton_Click"
                            Background="Transparent"
                            BorderThickness="0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border" Background="Transparent" CornerRadius="6" Width="28" Height="28">
                                    <Path Data="M6,6 L22,22 M22,6 L6,22"
                                          Stretch="Uniform" Width="12" Height="12"
                                          Stroke="#64748B" StrokeThickness="1.8"
                                          StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                          HorizontalAlignment="Center" VerticalAlignment="Center"
                                          x:Name="xPath"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#1E293B"/>
                                        <Setter TargetName="xPath" Property="Stroke" Value="White"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>

            <StackPanel Grid.Row="1" Margin="24,20,24,16">

                <Border Background="#1E293B"
                        CornerRadius="12"
                        Padding="20,16"
                        Margin="0,0,0,16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock x:Name="VersionText"
                                       Text="1.0.0 → 1.1.0"
                                       FontSize="22"
                                       FontWeight="Bold"
                                       Foreground="White"
                                       Margin="0,0,0,10"/>
                            <StackPanel Orientation="Horizontal">
                                <StackPanel Orientation="Horizontal" Margin="0,0,16,0">
                                    <TextBlock Text="&#xE7B8;" FontFamily="Segoe MDL2 Assets" FontSize="12" Foreground="#64748B" Margin="0,0,5,0"/>
                                    <TextBlock x:Name="FileSizeText"
                                               FontSize="12"
                                               Foreground="#64748B"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE787;" FontFamily="Segoe MDL2 Assets" FontSize="12" Foreground="#64748B" Margin="0,0,5,0"/>
                                    <TextBlock x:Name="ReleaseDateText"
                                               FontSize="12"
                                               Foreground="#64748B"/>
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>

                        <Border x:Name="RequiredBadge"
                                Grid.Column="1"
                                Background="#EF444420"
                                BorderBrush="#EF4444"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="10,5"
                                VerticalAlignment="Center"
                                Visibility="Collapsed">
                            <TextBlock Text="Обязательное"
                                       FontSize="11"
                                       FontWeight="Bold"
                                       Foreground="#EF4444"/>
                        </Border>
                    </Grid>
                </Border>

                <StackPanel x:Name="ChangelogSection">
                    <TextBlock Text="Что нового"
                               FontSize="11"
                               FontWeight="Bold"
                               Foreground="#475569"
                               Margin="0,0,0,8"/>

                    <Border Background="#1E293B"
                            CornerRadius="12"
                            Padding="16">
                        <ScrollViewer MaxHeight="170"
                                      VerticalScrollBarVisibility="Auto">
                            <ItemsControl x:Name="ChangelogList">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,0,0,6">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Border Grid.Column="0"
                                                    Width="5" Height="5" CornerRadius="2.5"
                                                    Background="#3B82F6"
                                                    Margin="0,7,10,0"
                                                    VerticalAlignment="Top"/>
                                            <TextBlock Grid.Column="1"
                                                       Text="{Binding}"
                                                       Foreground="#CBD5E1"
                                                       FontSize="13"
                                                       TextWrapping="Wrap"
                                                       LineHeight="20"/>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Border>
                </StackPanel>

                <Border x:Name="ProgressPanel"
                        Background="#1E293B"
                        CornerRadius="12"
                        Padding="20,18"
                        Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock x:Name="ProgressText"
                                   Text="Скачивание..."
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   Foreground="White"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,14"/>

                        <ProgressBar x:Name="DownloadProgress"
                                     Height="6"
                                     Minimum="0"
                                     Maximum="100"
                                     Value="0"
                                     BorderThickness="0"
                                     Background="#0F172A"
                                     Foreground="#3B82F6">
                            <ProgressBar.Template>
                                <ControlTemplate TargetType="ProgressBar">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="3">
                                        <Grid>
                                            <Border x:Name="PART_Track" CornerRadius="3"/>
                                            <Border x:Name="PART_Indicator"
                                                    Background="{TemplateBinding Foreground}"
                                                    HorizontalAlignment="Left"
                                                    CornerRadius="3"/>
                                        </Grid>
                                    </Border>
                                </ControlTemplate>
                            </ProgressBar.Template>
                        </ProgressBar>

                        <TextBlock Text="Не закрывайте окно — лаунчер закроется и перезапустится автоматически"
                                   FontSize="12"
                                   Foreground="#475569"
                                   TextWrapping="Wrap"
                                   TextAlignment="Center"
                                   Margin="0,12,0,0"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <Border Grid.Row="2"
                    Background="#0F172A"
                    CornerRadius="0,0,16,16"
                    BorderBrush="#1E293B"
                    BorderThickness="0,1,0,0"
                    Padding="24,14">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="LaterButton"
                            Grid.Column="0"
                            Content="Позже"
                            Height="40"
                            MinWidth="120"
                            Padding="16,0"
                            HorizontalAlignment="Left"
                            Background="Transparent"
                            Foreground="#64748B"
                            BorderBrush="#1E293B"
                            BorderThickness="1"
                            FontSize="13"
                            FontWeight="SemiBold"
                            Cursor="Hand"
                            Click="LaterButton_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="8">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#1E293B"/>
                                        <Setter Property="Foreground" Value="#94A3B8"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Opacity" Value="0.4"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>

                    <Button x:Name="UpdateButton"
                            Grid.Column="1"
                            Height="40"
                            MinWidth="160"
                            Padding="20,0"
                            Background="#22C55E"
                            Foreground="White"
                            BorderThickness="0"
                            FontSize="13"
                            FontWeight="Bold"
                            Cursor="Hand"
                            Click="UpdateButton_Click">
                        <Button.Content>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock x:Name="UpdateButtonIcon" Text="↓" FontSize="15" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" Margin="0,0,7,0"/>
                                <TextBlock x:Name="UpdateButtonText" Text="Обновить сейчас" FontSize="13" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button.Content>
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="border"
                                        Background="{TemplateBinding Background}"
                                        CornerRadius="8"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#16A34A"/>
                                    </Trigger>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter TargetName="border" Property="Background" Value="#1E293B"/>
                                        <Setter Property="Foreground" Value="#374151"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
